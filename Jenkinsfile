pipeline {
     agent any
     
     stages {
          stage("Build") {
               steps {
                    sh 'echo " Jldroid25 - Devops Engineer"'
               }
          }

          stage('Test') {
               steps {
                    sh 'echo "Test Api App !"'
                    
               }
          }

     }
}
